

const SectionTitle = ({heading}) => {
    return (
        <div>
           <h1 className="border-l-4 border-orange-500 text-3xl mt-20 pl-4 font-bold">{heading}</h1> 
        </div>
    );
};

export default SectionTitle;